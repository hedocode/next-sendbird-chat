

export default function Tabs(items) {
    return (
        <nav>
            { items && items.map(
                ({text, link}) => {
                    
                }
            )}
            <div></div>
        </nav>
    )
}